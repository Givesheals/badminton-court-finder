# Minimal deps for Streamlit frontend (Streamlit Community Cloud uses this file by default).
# The app only calls the Flask API over HTTP; no database or scrapers.
# For local/backend: use requirements-backend.txt
streamlit>=1.39.0
requests>=2.32.0
